import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Another Me - AI Avatar Generator",
  description: "Create stunning AI-generated avatars with our magical avatar generator. Transform your text descriptions into unique, personalized digital avatars.",
  keywords: "AI avatar generator, digital avatars, AI art, personalized avatars, avatar creation, AI generation, custom avatars",
  authors: [{ name: "Mainak Chaudhuri", url: "https://mainakfolio.netlify.app/" }],
  creator: "Mainak Chaudhuri",
  publisher: "Mainak Chaudhuri",
  robots: "index, follow",
  
 
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
        <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
        <meta name="theme-color" content="#020817" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}